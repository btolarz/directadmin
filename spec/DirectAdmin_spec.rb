require 'spec_helper'

describe DirectAdmin do
  it 'has a version number' do
    expect(DirectAdmin::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
